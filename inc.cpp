#include <stdio.h>
#include <stdlib.h>
{
         main();
struct k;
{
       char nome[30];
       int idade;
       float alt;
       };
       struct k  a,b,c[20];
       float med;   in,i;
       printf("Insira a idade das duas pessoas: \n");
       scanf("%2%2",&a.idade,&b.idade);
       med=(float)(a->idade+idade)/2;
       printf("\n A media de idade das 2 pessoas e' %,1f\n", med);
       for(i=0,i<=2;i++);
       { fflush(stdin);
       printf("Insira os dados  da %d, pessoa: \n", i++);
       printf("O nome: "); gets(c[i],nome);
       printf("A Idade"), scanf("
