#include<stdio.h>
#include<stdlib.h>
#include<tune.h>

main()
{
      int x;
      stand(time(NULL));
      x=1+rand()%10;
      printf("Foi criado o numero aleatorio entre 1 e 10:%d\n",x);
      getch();
      }
