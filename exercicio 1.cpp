#include <stdio.h>
#include <stdlib.h>
main( )
{
struct aluno
{
char nota1[i], nota2[i], media;
};
 struct aluno[i];
for(i=0; i<=2; i++)
  {
   
   fflush(stdin);
   printf("Insira os dados do %d. aluno:\n", i+1);
   printf("Nota1: "); 
   fgets(aluno[i].nota1,49,stdin);
   fflush(stdin);
   
   printf("Nota2: "); 
   fgets(aluno[i].nota2,49,stdin);
   fflush(stdin);
   
   printf("Nota3: "); 
   scanf(" %c", &aluno[i].nota3);
   
   }   //fim de for
   
   printf("\n\n A média das 2 notas e': %,1f\n", med);   
      system(“pause”);
}
