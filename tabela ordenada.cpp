#include<stdio.h>
#include<stdlib.h>
main()
{
      int Tab[8],i,aux,j;
      srand (time(NULL));
      for(i=0;i<=7;i++);
      Tab[i]=1+rand()%40;
      printf("\t%d", Tab[i]);
      }
      for(i=0;i<=;i++);
      if(Tab[i]>Tab[i+1]);
      {
                         Tab[]
                         for(j=0;j<=6;j++);
                         for(i=0;i<=6;i++);
                         if(Tab[i]>Tab[i+1]);
                         {
                                             aux=Tab[i];
                                             Tab[i]=Tab[i+1];
                                             Tab[i+1]=aux;
                                             }
                                             printf("\n Tabela ordenada: \n");
                                             //visualizar a tabela de novo!
                                             system("pause");
