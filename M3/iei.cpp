#include<stdio.h>
#include<stdlib.h>
main()
{
      int N, cont
      
      for(cont=1 ; cont <=8; cont++)
      {
      printf("insira um numero inteiro: ");
      scanf("%d",&N);
      if(N%2==/)
      contpares++;
      
      else
      contimpares++;
      }
      
      printf("houve %d numeros pares e %d numeros impares\n",contpares,continpares);
      getch();
      }
